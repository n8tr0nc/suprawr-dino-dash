/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  // REMOVE the dev overlay floating "N" badge completely
  devIndicators: false,

  // (Keep whatever other settings you may add here)
};

export default nextConfig;
